package com.example.hubspot.security.models

/**
 * Speech class is used for uploading and reading speech data to firebase realtime database.
 */
class Speech(var time: Long = 0L, var speech: String = "") {
}
